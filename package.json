{
  "name": "gildfall-chain-jam",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4188 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4188 --strictPort",
    "test": "node --test tests/*.test.js",
    "check:contract": "node scripts/check-contract.mjs"
  },
  "dependencies": { "@chain/casino-sdk": "file:vendor/casino-sdk", "viem": "^2.52.2" },
  "devDependencies": { "vite": "^7.3.0", "solc": "0.8.30", "ganache": "^7.9.2" }
}
